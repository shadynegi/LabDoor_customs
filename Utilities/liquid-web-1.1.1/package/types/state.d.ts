export interface LiquidWebState {
  glassSize: {
    width: number;
    height: number;
  };
  globalMousePos: {
    x: number;
    y: number;
  };
  isActive: boolean;
  isHovered: boolean;
  mouseOffset: {
    x: number;
    y: number;
  };
}
